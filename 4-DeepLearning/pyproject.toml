[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai_tools"
version = "0.1.0"
description = "Mes outils AI"
dependencies = []

[tool.setuptools]
packages = ["tools"]
